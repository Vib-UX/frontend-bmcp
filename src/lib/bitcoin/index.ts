/**
 * Bitcoin Command Encoder
 * Entry point for encoding Bitcoin → EVM commands
 */

export {
  BitcoinCommandEncoder,
  BitcoinFunctionEncoder,
  CHAIN_SELECTORS,
  type ChainName,
  type BitcoinOPReturnPayload,
  type CompactPayload,
  type FunctionCall,
} from './BitcoinCommandEncoder';

